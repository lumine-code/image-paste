{
  "name": "image-paste",
  "author": "lumine-code",
  "version": "1.0.0",
  "description": "Paste clipboard images into projects as image files.",
  "keywords": [
    "clipboard",
    "screenshot",
    "png",
    "insert",
    "upload"
  ],
  "repository": "https://github.com/lumine-code/image-paste",
  "bugs": {
    "url": "https://github.com/lumine-code/image-paste/issues"
  },
  "license": "MIT",
  "main": "./lib/main",
  "files": [
    "lib",
    "spec",
    "styles"
  ],
  "engines": {
    "lumine": "^1.0.0"
  },
  "backgroundTips": [
    "{% if keys['image-paste:paste'] %}You can paste an image from the clipboard into the project with {{ 'image-paste:paste' | keystroke }}{% else %}You can paste an image from the clipboard straight into the project and have its relative path inserted.{% endif %}"
  ],
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "configSchema": {
    "assetsDirectory": {
      "title": "Assets Directory",
      "description": "Editor-relative directory used when saving images pasted into text editors.",
      "type": "string",
      "default": "assets"
    },
    "forwardSlash": {
      "title": "Use Forward Slashes",
      "description": "Insert image paths with forward slashes in text editors.",
      "type": "boolean",
      "default": true
    },
    "imagePreview": {
      "title": "Image Preview",
      "description": "Show an image preview in the save dialog.",
      "type": "boolean",
      "default": true
    }
  },
  "consumedServices": {
    "tree-view.selection": {
      "versions": {
        "^1.0.0": "consumeTreeViewSelection"
      }
    }
  },
  "dependencies": {
    "@electron/remote": "2.1.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.2.2",
    "globals": "^17.7.0",
    "prettier": "^3.9.6"
  }
}
